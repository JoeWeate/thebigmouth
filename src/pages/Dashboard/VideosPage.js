import React from "react";

const VideosPage = ({ state, data }) => {
  return (
    <div
      style={{
        backgroundColor: "grey",
      }}
    >
      {state}
    </div>
  );
};

export default VideosPage;
