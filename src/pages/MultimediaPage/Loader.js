import {Typography} from "@mui/material";
import React from "react";

const Loader = () => {
    return  <Typography sx={{color: "white"}} variant="h5" component='p'>Data is loading...</Typography>;
};

export default Loader;
