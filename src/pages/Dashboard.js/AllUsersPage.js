import React from "react";

const AllUsersPage = ({ state }) => {
  return (
    <div
      style={{
        backgroundColor: "green",
      }}
    >
      {state}
    </div>
  );
};

export default AllUsersPage;
