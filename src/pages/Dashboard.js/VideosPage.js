import React from "react";

const VideosPage = ({ state }) => {
  return (
    <div
      style={{
        backgroundColor: "grey",
      }}
    >
      {state}
    </div>
  );
};

export default VideosPage;
